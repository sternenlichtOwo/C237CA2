<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
        <title>disabled database</title>
    </head>
<body>
    <nav class="navbar navbar-expand-sm bg-dark navbar-dark">
        <div class="container-fluid">
          <a class="navbar-brand" href="/">database</a>
          <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#collapsibleNavbar">
            <span class="navbar-toggler-icon"></span>
          </button>
          <div class="collapse navbar-collapse" id="collapsibleNavbar">
            <ul class="navbar-nav">
              <li class="nav-item">
                <a class="nav-link" href="/addData">Add new data</a>
              </li> 
            </ul>
          </div>
        </div>
    </nav>

    <div class="container">
        <br>
        <div class="text-center"><h2>Update Database</h2></div>
        <br>
        <div class="text-center">
          <% if (patient) { %> 
            <form action="/editdata/<%= patient.patient_id %>" method="POST" enctype="multipart/form-data">
                <label for="name">patient Name:</label><br>
                <input type="text" id="name" name="name" value = "<%= patient.full_name %>" required><br><br>

                <label for="DOB">Date of birth:</label><br>
                <input type="date" id="DOB" name="DOB" value = "<%= patient.date_of_birth %>" required><br><br>

                <label for="gender">gender:</label><br>
                <input type="text" id="gender" name="gender" value = "<%= patient.gender %>" required><br><br>

                <label for="address">address:</label><br>
                <input type="text" id="address" name="address" value = "<%= patient.address %>" required><br><br>

                <label for="contact">contact:</label><br>
                <input type="text" id="phone" name="phone" value = "<%= patient.phone %>" required><br><br>

                <label for="next_of_kin">next_of_kin:</label><br>
                <input type="text" id="next_of_kin" name="next_of_kin" value = "<%= patient.next_of_kin %>" required><br><br>

                
                <button type="submit">Update patient</button>
            </form>
          <% } else { %>
              <p>No patient found.</p>
          <% } %>
          <a href ="/">Back</a>
        </div>
    </div>
</body>
</html>
